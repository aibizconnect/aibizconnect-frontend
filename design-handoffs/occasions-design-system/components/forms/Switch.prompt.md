**Switch** — instant on/off toggle for settings (notifications, automations, feature flags). Use a Checkbox instead when the change only applies on form submit.

```jsx
<Switch label="Auto follow-up sequence" defaultChecked />
<Switch label="Public booking page" description="Let clients self-schedule" />
```

- Springy knob animation. Sizes `sm | md`. Brand-blue when on.
