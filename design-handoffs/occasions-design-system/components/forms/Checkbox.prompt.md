**Checkbox** — boolean choice with optional label and description line. Works controlled or uncontrolled.

```jsx
<Checkbox label="Email me lead alerts" defaultChecked />
<Checkbox label="SMS reminders" description="Standard message rates apply" />
```

- Brand-blue fill when checked. Use `description` for settings rows.
