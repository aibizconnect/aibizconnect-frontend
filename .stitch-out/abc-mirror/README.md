# ABC SalesMaster (aibizconnect.app) — full capture, 2026-06-12

Lovable-built React SPA, captured before hosting expiry.

- index.html + assets/index.js (3.7 MB bundle, ALL 235 routes) + assets/index.css + images + favicon + og-image
  = the COMPLETE static app. Rehostable as-is on any static host (e.g. Cloudflare Pages):
  serve this folder, route all paths to index.html.
- rendered/*.html = browser-rendered snapshots of the 6 public marketing pages with
  computed-style (data-cs) annotations — the import source of record.
- ROUTES.txt = every route found in the bundle.
- The 6 marketing pages were ALSO translated to native editable pages in the builder:
  website "ABC SalesMaster (imported)" (e53089f3-b078-4ef9-8e04-aba951ef520f) under
  tenant AIBizConnect Consulting. Inspector 100/100 each.
- NOT capturable from outside: the Lovable project source + its Supabase data. Ali should
  use Lovable's GitHub sync / code export before expiry for a perfect source copy.
