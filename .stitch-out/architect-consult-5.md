Ali's morning feedback on the lossless editor (screenshots reviewed): (1) Layers panel shows THREE "Header" sections (imported-css carrier mislabeled + nav band + the hero which Stitch marks up as <header> tag); (2) clicking an element inside a band leaves the RIGHT inspector empty — only the band-level fields show; (3) DIRECTIVE: "all these HTML code shall be converted to those elements, or those elements shall contain these pieces of HTML — translate the HTML tags to match the elements. <img> is the element Image." He wants imported nodes to surface as OUR native elements with the standard inspector.

PROPOSED: ELEMENT PROJECTION (facade) over the immutable HTML — not conversion:
- Selecting a node inside a band PROJECTS it to a native element content object: h1-h6→heading{text,level,color,fontSize}, p/span leaf→text{...}, img→image{url,alt,width,rounding}, a→button{label,href,bgColor,textColor}; prefill from the node's data-cs computed styles.
- The RIGHT panel renders our existing SectionEditor (same inspector as native elements — General/Styles UX Ali knows) for the projected content.
- onUpdate DIFFS projected fields → patches: text→{op:text}, url→{op:image}, href→{op:link}, styleish keys→{op:style} with CSS mapping (fontSize→font-size px, bgColor→background-color, rounding→border-radius...). Unmappable fields hidden or ignored.
- Structural ops already exist (move/duplicate/remove patches). HTML stays source of truth (D-178 intact); projection is a VIEW, conversion never happens silently (D-182 opt-in remains).
- Fixes in same pass: bandName (a <header> containing h1/h2 → named by its heading, only nav/first-header → "Header"; css carrier labeled "Design CSS" and hidden from Layers), and the page-structure panel rendering "Row 1" children for imported bands.

QUESTIONS: (a) Ratify projection-as-view vs Ali's literal "convert to elements" (I argue conversion = the lossy translator we just escaped; projection gives identical UX without fidelity loss — but flag if you disagree). (b) For unmappable inspector fields (e.g. heading 'level' would restructure the tag): hide them, or apply via tag-rewrite patch op? (c) Priority of supporting form fields/menus as projected composites vs the 4 leaf types now? Terse, numbered.
