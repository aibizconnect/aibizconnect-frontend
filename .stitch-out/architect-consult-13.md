BUILDER CONSULT + RULING TO FILE (Ali, verbatim force): "I want you to teach Bill to translate what it sees in the HTML to what WE have — it sees a row with a 4-column row inside it, it should look like ours, using OUR elements. Don't make anything up. We won't have ANYTHING in the Tree or in the Canvas that is not one of our elements. Keep this as a RULE."

THE RULE (file as supreme over D-178's lossless-default): Bill's editing model is NATIVE-ELEMENTS-ONLY. The translator (htmlToSections faithful — already carrying D-169..D-176: outermost-grid recursion, column chunking, DOM-order splice, wrapper-bg climb, sibling labels, button chrome, menu groups) becomes the DEFAULT import again; its output is native Sections → OUR Row elements (real editable cells, "+ Add element", full inspectors) → our elements with captured styles. Unknown wrappers flatten; unrepresentable fragments fall back to the Custom HTML element (one of ours). The lossless capture (bridge data-cs/uid stamping, CSS snapshot) REMAINS as the internal fidelity step feeding the translator + future re-capture; the imported-html band path and its iframe/projection editing stand down as the default UX (kept in code as the fidelity reference/backstop).

ACTION PLAN (executing now): flip importHtmlAsDraftPage default mode to the translator; re-import Ottawa natively (clearing the page-level design CSS, since native elements style themselves + theme); verify the known-good bands (header row+menu+gold CTA, 2x2 about, navy contact card, footer order — all previously verified in diag); Inspector adjusts (css-missing check only applies to imported-html pages). Rule on: (1) anything you want preserved from the lossless UX before it stands down (the Edits/patch audit doesn't apply to native—native has undo/history); (2) keep storing the raw capture on the page for future re-translate? Update ALL blueprint sections to subordinate to this RULE.
